import { test } from "@playwright/test";

test.describe("@advanced TestGroup", () => {
  test("A", async ({ page }) => {});

  test("B", async ({ page }) => {});

  test("C", async ({ page }) => {});
});
